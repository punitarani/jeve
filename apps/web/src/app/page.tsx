import { Landing } from "@/components/Landing";

// WEB-0005: a static shell. Everything that moves — the hero's clock, the
// dashboard, the first fetch — happens client-side.
export default function Page() {
	return <Landing />;
}
