<!-- GENERATED by scripts/gen-decisions.py. Do not edit. -->

# Decision index

One row per decision record. Read this first, then open only the
records whose scope covers what you are about to change.

| ID | Status | Scope | Title | Summary |
| --- | --- | --- | --- | --- |
| [API-0001](../py/src/jeve/api/decisions/API-0001-seq-cursor-everywhere.md) | accepted | `py/src/jeve/api/app.py`<br>`apps/web/src/lib/api.ts` | Make the event seq the only cursor the client tracks | Every endpoint that returns events also returns the `seq` it is current as of, and `/stream` takes `after=<seq>`, so a reconnect is exactly "give me what comes after this" with no gap and no duplicate. |
| [API-0002](../py/src/jeve/api/decisions/API-0002-the-seq-cursor-pages-both-ways.md) | accepted | `py/src/jeve/api/app.py`<br>`py/src/jeve/api/contracts.py`<br>`apps/web/src/lib/api.ts`<br>`apps/web/src/components/Dashboard.tsx` | Page the seq cursor both ways, and read the timeline newest first | `/events` takes `before=<seq>` as well as `after=<seq>`, and every page carries a cursor at each end — `seq` (newest) and `oldest` — plus `more`, which says whether another page exists in the direction this one travelled. Rows come back ascending whichever way the window was taken; the timeline reverses once, where it renders. |
| [API-0003](../py/src/jeve/api/decisions/API-0003-the-field-report-is-one-endpoint-memoised-per-tick.md) | accepted | `py/src/jeve/api/report.py`<br>`py/src/jeve/api/app.py`<br>`py/tests/test_api.py` | The field report is one endpoint, memoised per tick | `GET /report` returns every aggregate the report draws, in one `FieldReport`, and computes it at most once per tick per process. |
| [API-0004](core/API-0004-degeneracy-detectors-are-part-of-the-field-report-and-each-is-seen-to-fire.md) | accepted | `py/src/jeve/api/detectors.py`<br>`py/src/jeve/api/report.py`<br>`py/src/jeve/api/contracts.py`<br>`py/tests/test_detectors.py`<br>`apps/web/src/components/report/LiveReport.tsx` | Degeneracy detectors are part of the field report, and each is seen to fire | `FieldReport.detectors` carries eight readings from `jeve.api.detectors`, each with a value, the line past which it fires, and the rows it came from; the live report shows them first. |
| [CORE-0001](core/CORE-0001-record-architecture-decisions.md) | accepted | `decisions/**`<br>`**/decisions/*.md`<br>`scripts/gen-decisions.py` | Record architecture decisions as immutable per-file records | Decisions are MADR 4.0 records, one per file, named `<PREFIX>-NNNN-kebab.md`, with machine-readable front matter; `INDEX.md`, `index.jsonl`, `schema.json` and the AGENTS.md decision section are generated from them and never hand-edited. |
| [CORE-0002](core/CORE-0002-budget-governs-clock-speed.md) | accepted | — | Budget is the independent variable and clock speed is derived from it | Model spend has a hard ceiling per real day, and the simulation's clock speed is whatever that ceiling affords; the unit of cost is the decision point, not the tick. |
| [CORE-0003](core/CORE-0003-simulated-clock.md) | accepted | — | Run on a simulated clock advanced by a discrete-event scheduler | Sim time is an integer count of sim-seconds from a seeded epoch, advanced only inside the tick transaction, with a 15-sim-minute scheduler quantum and pacing set by the budget governor. |
| [CORE-0004](core/CORE-0004-backpressure-slows-the-clock.md) | accepted | `py/src/jeve/llm/**` | Slow the clock under backpressure; never default a decision | The tick is a barrier: sim time does not advance until every decision due at that tick has a real answer, so a slow model means a slow world and a dead model means a paused world. |
| [CORE-0005](core/CORE-0005-path-derived-randomness.md) | accepted | — | Replay from the recorded log, and derive every random draw from a path | A recorded run replays to a byte-identical event log from its stored model responses, never by re-querying; every random draw is a pure function of a path (`seed, agent, decision_seq, question`), so no RNG state exists to persist. |
| [CORE-0006](core/CORE-0006-one-python-distribution.md) | superseded | `py/pyproject.toml`<br>`py/src/jeve/**` | One Python distribution with layered subpackages, inside an Nx workspace | Python is a single `jeve` distribution whose subpackages are layered (`contracts → world → decide/memory → gen → sim/api`), with an import-layering test as the boundary, and Nx sees three projects: `py`, `web`, `contracts`. |
| [CORE-0007](core/CORE-0007-postgres-one-transaction-per-tick.md) | accepted | — | Persist to Postgres with one transaction per tick and a causal event log | Every tick commits its domain mutations, events, decisions, scheduler changes and clock advance in a single transaction; events are append-only and carry a `causes` array of the event ids that led to them. |
| [CORE-0008](core/CORE-0008-the-layering-the-test-enforces-is-the-layering.md) | accepted | `py/pyproject.toml`<br>`py/src/jeve/**`<br>`py/tests/test_layering.py` | The layering the test enforces is the layering | One `jeve` distribution; its packages are layered `core -> llm -> decide -> world -> sim`, with `gen` and `api` on the outside. `py/tests/test_layering.py` is the definition: its `FORBIDDEN` table is the layering, walked over the real import graph, lazy imports included. Two rows carry the weight. Nothing that computes the world may import `gen` (sim state never depends on generated text), and `world` may not import `llm` (the engine reaches a model only through the `Policy` seam). |
| [CORE-0009](core/CORE-0009-randomness-is-keyed-by-what-it-is-about.md) | accepted | `py/src/jeve/world/**`<br>`py/src/jeve/core/seed.py`<br>`py/tests/test_world.py` | Randomness is keyed by what it is about, never by when it was drawn | A seed path names the thing the draw is *about* and, where the thing recurs, its own period: an invoice amount is `(issuer, client, billing month)`; whether a subscriber notices an outage is `(person, incident)`; the hour a payer sits down to their bills is `(person, day)`; the hazard is `(module, day, slot)`; where someone stands is `(person, zone, arrival)`. `tick_seq` and event sequence numbers never appear in a seed path, and a test walks the source to keep it so. |
| [CORE-0010](core/CORE-0010-nx-python-integration-via-nxlv-python-plugin.md) | proposed | `nx.json`<br>`py/project.json`<br>`apps/api/project.json`<br>`apps/sim/project.json` | Nx Python integration via @nxlv/python plugin | Use @nxlv/python plugin for Nx Python integration, with fallback to manual project.json configuration if the plugin becomes unmaintained. |
| [CORE-0011](core/CORE-0011-the-api-contract-is-pydantic-in-the-package-generated-to-zod-and-tested-against-the-wire.md) | accepted | `py/src/jeve/api/contracts.py`<br>`tools/contract-gen/**`<br>`packages/contracts/src/index.ts`<br>`py/tests/test_api.py` | The API contract is pydantic in the package, generated to zod, and tested against the wire | `py/src/jeve/api/contracts.py` is the one source of truth for what the API serves: real pydantic models inside the `jeve` package, where `mypy --strict` and the layering test see them. `tools/contract-gen/generate_zod.py` renders `packages/contracts/src/index.ts` from them; `test_api.py` validates live endpoint responses against the same models, so the models cannot drift from the SQL without a failing test. Field descriptions and docstrings carry the comments into the generated file — no override table. A field the API omits rather than sends as JSON null is `X \| None` with a default (`.optional()`); a key that is present and null is required `X \| None` (`.nullable()`). |
| [DECIDE-0001](../py/src/jeve/decide/decisions/DECIDE-0001-one-request-per-decision-point.md) | accepted | — | Send one request per decision point carrying the whole decision surface | An agent is evaluated only at a decision point — task complete, inbox item over an importance floor, schedule boundary, synchronous interaction, or a mandatory timer — and that one request carries every question the role might need. |
| [DECIDE-0002](../py/src/jeve/decide/decisions/DECIDE-0002-two-tier-escalation.md) | accepted | — | Escalate on confidence in two tiers, and keep the first tier typed | Tier 1 re-asks only the uncertain questions of a flash LLM through TypeSafe's adapter, which returns the same typed response shape; tier 2 is generative and fires only on an ontology gap or when a viewer needs prose. |
| [DECIDE-0003](../py/src/jeve/decide/decisions/DECIDE-0003-question-sets-sampling-and-the-call-cache.md) | superseded | `py/src/jeve/decide/questions.py`<br>`py/src/jeve/decide/sampling.py`<br>`py/src/jeve/decide/recorder.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/fixtures/cassettes/**` | Question sets in words, J/P/H resolution, and a content-hash call cache | State is rendered in words with traits bucketed into tertiles of their seeded range; every question is tagged J (argmax), P (sampled) or H (a day-level propensity thinned to a per-tick hazard); hard constraints are code gates that never reach the model; and every call is cached in `model_calls` by the content hash of `(model, state, questions)`, mirrored to a sorted JSONL cassette. |
| [DECIDE-0004](../py/src/jeve/decide/decisions/DECIDE-0004-the-cache-key-is-the-bytes-sent-and-the-version-that-answered.md) | accepted | `py/src/jeve/llm/protocol.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/src/jeve/llm/catalog.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/src/jeve/decide/recorder.py`<br>`py/fixtures/cassettes/**` | The cache key is the bytes that were sent and the model version that answered | `DecisionRequest.wire_bytes()` is the one place a decision body is built. The gateway posts exactly those bytes; the policy hashes exactly those bytes. |
| [DECIDE-0005](../py/src/jeve/decide/decisions/DECIDE-0005-tier-1-runs-in-shadow-inside-the-tick-and-a-set-goes-live-by-name.md) | accepted | `py/src/jeve/decide/escalation.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/src/jeve/decide/policy.py`<br>`py/src/jeve/decide/recorder.py`<br>`py/src/jeve/sim/escalations.py`<br>`py/src/jeve/sim/panel.py`<br>`py/src/jeve/gen/ontology.py`<br>`py/migrations/0013_escalations.sql`<br>`py/tests/test_escalation.py`<br>`py/tests/test_measurement.py` | Tier 1 runs in shadow inside the tick, and a set goes live by name | Tier 1 is asked inside `JevPolicy.decide_many`, between Jev's answer and the draw, off unless `JEVE_ESCALATION` says `shadow` or `live`; it acts only for sets named in `JEVE_ESCALATION_LIVE`. |
| [DECIDE-0006](../py/src/jeve/decide/decisions/DECIDE-0006-route-a-question-set-to-tier-1-outright-by-name-when-evals-show-jev-under-answers-it.md) | accepted | `py/src/jeve/decide/escalation.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/src/jeve/config.py`<br>`py/src/jeve/sim/runner.py`<br>`py/tests/test_escalation.py` | Route a question set to tier 1 outright, by name, when evals show Jev under-answers it | `JEVE_ESCALATION_ROUTE` names sets — or single questions, as `set:ask` — that tier 1 answers outright in Jev's typed shape, sampled from the LLM's own distribution; it defaults to `episode.round`. |
| [EVAL-0001](core/EVAL-0001-an-llm-role-is-kept-only-on-a-held-out-judged-paired-a-b.md) | accepted | `py/src/jeve/evals/**`<br>`py/scripts/evals.py`<br>`py/tests/test_evals.py`<br>`py/tests/test_layering.py` | An LLM role is kept only on a held-out, judged, paired A/B | An LLM role is kept only when its held-out, paired-by-seed contrast against the arm without it shows a gain and no clear loss, and a judge that has passed a planted-defect test prefers it. |
| [EVAL-0002](core/EVAL-0002-staff-stay-seeded-an-llm-written-cast-flattened-them.md) | accepted | `py/src/jeve/world/seed_world.py` | Staff stay seeded; an LLM-written cast flattened them | Staff traits stay seeded: the LLM-written cast lowered persona signal on held-out seeds and bought nothing the judge could see. |
| [GEN-0001](../py/src/jeve/gen/decisions/GEN-0001-dialogue-is-a-projection.md) | accepted | `py/src/jeve/gen/**`<br>`py/tests/test_layering.py`<br>`py/tests/test_dialogue.py` | Dialogue is a projection — rendered on click, cached by content hash, never read back | `GET /encounters/{seq}/dialogue` always returns the typed record, and renders two to four lines of dialogue only when asked: from the first reachable escape-hatch model (LLM-0005), cached in `model_calls` under the content hash of the request, and imported by nothing that computes the world. |
| [LLM-0001](../py/src/jeve/llm/decisions/LLM-0001-openrouter-for-everything.md) | accepted | `py/src/jeve/llm/gateway.py`<br>`py/src/jeve/llm/catalog.py` | Reach every model through OpenRouter, including Jev | Everything goes through OpenRouter: chat completions at `/api/v1/chat/completions` and Jev at `/api/alpha/decisions`, under one key with one spend meter. |
| [LLM-0002](../py/src/jeve/llm/decisions/LLM-0002-model-preference-and-slug-resolution.md) | superseded | `py/src/jeve/llm/catalog.py` | Resolve model slugs against the live catalogue and fail loudly | Every configured slug is resolved against OpenRouter's live catalogue before any call, and an unresolved name is fatal with near-matches printed. |
| [LLM-0003](../py/src/jeve/llm/decisions/LLM-0003-per-request-provider-preferences.md) | accepted | `py/src/jeve/llm/protocol.py` | Route with per-request provider preferences, never account settings | Routing preferences are set per request in the body — ordering, sort, ignore list — and account-level settings are left alone; if one ever must change, the before-state is written to `ops/provider-settings.before.json` first. |
| [LLM-0004](../py/src/jeve/llm/decisions/LLM-0004-budget-guard.md) | superseded | `py/src/jeve/llm/budget.py`<br>`py/src/jeve/llm/ledger.py` | Guard spend with reservations in a locked ledger at the repo root | Every call reserves its worst-case cost in an append-only locked ledger before it is issued and settles at the real cost afterwards; effective spend counts settled costs plus every reservation that never settled. |
| [LLM-0005](../py/src/jeve/llm/decisions/LLM-0005-escape-hatch-order-and-per-path-slug-resolution.md) | superseded | `py/src/jeve/llm/catalog.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/scripts/probe_providers.py` | Escape-hatch order and per-path slug resolution | Slugs are still resolved against the live catalogue at startup, but per path: an unknown decision slug is fatal, an unknown generative slug drops out of the preference list, and only a generative list that resolves to nothing is an error — raised when prose is requested, not at startup. |
| [LLM-0006](../py/src/jeve/llm/decisions/LLM-0006-dialogue-order-by-measured-reliability-and-52x-retryable.md) | accepted | `py/src/jeve/llm/catalog.py`<br>`py/src/jeve/llm/gateway.py` | Dialogue model order by measured reliability, and 52x is retryable | The generative order is GLM 5.3 Flash, Gemini 3.8 Flash, GPT-5.6 Luna, DeepSeek V4 Pro (0813), DeepSeek V4.1 Flash. Per-path resolution from LLM-0005 stands: a decision slug that does not resolve is fatal, a generative slug that does not resolve is skipped. |
| [LLM-0007](../py/src/jeve/llm/decisions/LLM-0007-the-spend-ledger-lives-in-postgres-and-openrouter-holds-the-ceiling.md) | superseded | `py/src/jeve/llm/ledger.py`<br>`py/src/jeve/llm/budget.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/migrations/0008_spend_ledger.sql`<br>`py/tests/test_ledger_and_budget.py` | the spend ledger lives in Postgres and OpenRouter holds the ceiling | `spend_ledger` is an append-only table; `SpendLedger` keeps its reserve/settle/release/baseline/remote API with `read()` as SQL aggregation, and `authorise` holds a transaction-level advisory lock across read+reserve. Ceilings are env-tunable and set high; OpenRouter's 402 — surfaced as `ProviderBudgetError` — is the real stop, and the daemon answers it with `waiting_on_budget`, not a halt (SIM-0003). |
| [LLM-0008](../py/src/jeve/llm/decisions/LLM-0008-trace-every-model-call-to-braintrust-from-one-module-off-by-default.md) | superseded | `py/src/jeve/tracing.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/src/jeve/api/app.py`<br>`py/tests/test_tracing.py` | trace every model call to braintrust, from one module, off by default | Model calls are traced to Braintrust from a single guarded module, `jeve.tracing`, which is a no-op unless `BRAINTRUST_API_KEY` is set. |
| [LLM-0009](../py/src/jeve/llm/decisions/LLM-0009-one-trace-per-tick-and-each-batch-carries-its-decisions.md) | accepted | `py/src/jeve/tracing.py`<br>`py/src/jeve/world/engine.py`<br>`py/src/jeve/decide/jev_policy.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/src/jeve/api/app.py`<br>`py/src/jeve/sim/daemon.py`<br>`py/src/jeve/config.py`<br>`py/tests/test_tracing.py`<br>`py/tests/test_jev_policy.py`<br>`py/tests/test_daemon.py`<br>`py/tests/test_dialogue.py` | one trace per tick, and each batch carries its decisions | Every sim tick is one Braintrust trace, from a single guarded module (`jeve.tracing`) that is a no-op unless `BRAINTRUST_API_KEY` is set. Each decision batch under the tick logs every decision's facts as input and, as output, what was chosen and whether it was gated, cached or live. Live model calls and their HTTP tries nest beneath. |
| [LLM-0010](../py/src/jeve/llm/decisions/LLM-0010-the-ledger-keeps-a-running-total-and-a-spend-check-reads-one-row.md) | accepted | `py/src/jeve/llm/ledger.py`<br>`py/src/jeve/llm/budget.py`<br>`py/src/jeve/llm/gateway.py`<br>`py/migrations/0011_spend_totals_and_read_indexes.sql`<br>`py/tests/test_ledger_and_budget.py` | the ledger keeps a running total, and a spend check reads one row | `spend_entries` stays append-only and every writer still takes the advisory lock across read-modify-write (LLM-0007's rules stand); what a spend check reads is the one row of `spend_totals`, which every append moves by exactly what the fold would, and the fold survives as `rebuild()`: the migration's seed, the repair, and what a ledger runs on open when the total is behind the ledger's head. |
| [MEM-0001](../py/src/jeve/memory/decisions/MEM-0001-relational-retrieval-typed-beliefs.md) | accepted | — | Retrieve memory relationally, revise beliefs typed, and compact in SQL | Relevance is a join over typed entity references rather than a similarity search; importance and belief revision are Jev `score` questions batched into the decision request; and compaction is SQL aggregation, not generation. |
| [MEM-0002](../py/src/jeve/memory/decisions/MEM-0002-facts-travel-and-promises-are-scored.md) | accepted | `py/src/jeve/memory/**`<br>`py/migrations/0009_episodes.sql`<br>`py/tests/test_episodes.py` | Facts travel, promises are scored, and both are typed rows | A **fact** is a thing that can be known and passed on, identified by what it is about (`outage:<incident>`, `price_rise:<org>`) rather than minted from a counter, so the same news is the same row in every arm of a counterfactual. **Knowledge** records who holds it, from whom, and at what remove. A **commitment** is a promise to pay a particular bill. |
| [MEM-0003](../py/src/jeve/memory/decisions/MEM-0003-trust-is-a-typed-belief-and-a-fact-can-be-false.md) | accepted | `py/src/jeve/memory/**`<br>`py/src/jeve/world/customers.py`<br>`py/migrations/0012_loops.sql` | Trust is a typed belief, and a fact can be false | A subscriber's trust in the vendor is `persons.beliefs.vendor_reliability`, 0 to 4, revised by a J `score` after each outage they ran into and recovering a level after a month without one; it gates renewal. Facts can now be about late wages, a firm short of money, a vendor losing customers or a broken promise, and `true_fact` is false for a rumour, which travels like any fact. |
| [MEM-0004](../py/src/jeve/memory/decisions/MEM-0004-who-has-met-whom-is-a-typed-tie-and-it-shapes-who-is-approached.md) | accepted | `py/src/jeve/memory/ties.py`<br>`py/migrations/0015_signal.sql`<br>`py/src/jeve/decide/policy.py`<br>`py/src/jeve/world/space.py`<br>`py/tests/test_ties.py`<br>`py/tests/test_query_plans.py` | Who has met whom is a typed tie, and it shapes who is approached | `ties` holds one row per pair (`a < b`): meetings, warmth, first and last meeting, and the last topic. Encounters, episodes and promises write it, and questions read it as words. |
| [OPS-0001](../infra/decisions/OPS-0001-deployment-topology-fly-process-groups-workers-static-assets-doppler-secrets.md) | accepted | `infra/**`<br>`fly.toml`<br>`docker-compose.prod.yml`<br>`apps/web/wrangler.toml`<br>`.github/workflows/ci.yml`<br>`Makefile` | deployment topology — fly process groups, workers static assets, doppler secrets | `fly.toml` is one app `jeve-backend` with process groups `api` (the only ingress, on `JEVE_DATABASE_POOLED_URL` when set, else the direct DSN) and `sim` (`sim-entrypoint.sh`, `on-failure` restarts, no service). The web app is an assets-only Worker serving `out/`. Secrets live in Doppler projects `app`/`infra`/`worker` and land as Fly secrets and GitHub secrets; the sim's Postgres URL must be a *direct* connection because the writer lock is a session-level advisory lock. The frontend is read-only, so the dialogue endpoint's spend path is off in production (`JEVE_DIALOGUE_GENERATE=off`). |
| [OPS-0002](../infra/decisions/OPS-0002-run-the-suite-once-per-ci-run-and-confirm-records-by-collection.md) | accepted | `.github/workflows/ci.yml`<br>`scripts/run-confirmations.py`<br>`scripts/test_run_confirmations.py`<br>`scripts/ci-needs-python.sh`<br>`py/tests/conftest.py` | Run the suite once per CI run, and confirm records by collection | The suite runs once per CI run under `pytest -n auto --dist loadfile`, each xdist worker on a database of its own, and `scripts/run-confirmations.py` then runs each record's `confirmation` once — in `--pytest collect` mode a command that is nothing but `cd py && uv run pytest <paths>` is collected rather than re-executed. |
| [OPS-0003](../infra/decisions/OPS-0003-deploy-the-web-app-after-the-backend-never-before-it.md) | accepted | `.github/workflows/ci.yml`<br>`scripts/check_deploy_order.py` | Deploy the web app after the backend, never before it | `deploy-web` needs `[web, contracts, deploy-backend]`, so the page ships only once the API it talks to is live. |
| [OPS-0004](../infra/decisions/OPS-0004-agents-read-production-through-their-own-doppler-project-and-a-role-that-cannot-write.md) | proposed | `.claude/setup-cloud.sh`<br>`docs/environment-variables.md` | Agents read production through their own Doppler project and a role that cannot write | Agents read production through a token for `agent/prd`: its database URLs name a role inheriting only `pg_read_all_data`, and its OpenRouter key is its own, not production's. |
| [SIM-0001](../py/src/jeve/sim/decisions/SIM-0001-one-run-loop-a-horizon-and-a-governor.md) | accepted | `py/src/jeve/sim/**`<br>`py/scripts/run_fixture.py` | One run loop, a horizon, a single-writer lock, and a governor that pauses | `python -m jeve.sim` is the only run loop; the fixture is that loop with pacing off and a horizon. `--until` makes a wall-clock process replayable, a Postgres advisory lock makes it the only writer, and the budget governor pauses the clock until the window rolls over rather than changing how anything behaves. |
| [SIM-0002](../py/src/jeve/sim/decisions/SIM-0002-the-daemon-outlives-its-model.md) | accepted | `py/src/jeve/sim/daemon.py`<br>`py/src/jeve/sim/runner.py`<br>`py/src/jeve/api/app.py`<br>`py/migrations/**` | The daemon outlives its model - wait, say so, retry the tick | The run loop treats `TransportError`, `ResponseShapeError` and `TimeoutError` as weather. The tick is rolled back, `sim_meta.status` becomes `waiting_on_model` with the error's class and first line in `last_error`, and the same tick is tried again after a jittered backoff that doubles to a cap of two minutes. The first tick that succeeds sets `running` and clears the error. Budget exhaustion and a replay miss keep their own statuses and exits: they are not weather, and retrying them cannot help. |
| [SIM-0003](../py/src/jeve/sim/decisions/SIM-0003-waiting-on-budget-is-a-status-and-deliberate-halts-exit-clean.md) | accepted | `py/src/jeve/sim/daemon.py`<br>`py/migrations/0007_waiting_on_budget.sql`<br>`infra/docker/sim-entrypoint.sh`<br>`py/tests/test_daemon.py` | waiting on budget is a status, and deliberate halts exit clean | A 402 raises `ProviderBudgetError`; the daemon rolls the tick back, writes `waiting_on_budget` to `sim_meta`, beats from the sleep loop, and retries the same tick on a minutes-scale interval (`JEVE_BUDGET_WAIT_S`). In the container, `sim-entrypoint.sh` maps exits 4–7 to 0, so `on-failure` restarts crashes and leaves deliberate halts down; the reason stays in `sim_meta`, which is where an operator looks anyway. |
| [SIM-0004](../py/src/jeve/sim/decisions/SIM-0004-the-daemon-outlives-its-database-and-the-night-is-a-dial.md) | accepted | `py/src/jeve/sim/daemon.py`<br>`py/tests/test_daemon.py` | the daemon outlives its database, and the night is a dial | Dead-time speedup is `JEVE_NIGHT_SPEEDUP` / `--night-speedup`, and losing the database is weather in the sense of SIM-0002: the daemon takes another connection and runs the same tick again, for ever, rather than exiting. |
| [WEB-0001](../apps/web/decisions/WEB-0001-causal-timeline-not-a-map.md) | superseded | — | Show a state and event dashboard built around a causal timeline, not a map | The centrepiece is a causal timeline — events in lanes per org with their `causes` links drawn — beside an org graph of typed flows and a person panel showing the distribution a decision came from and the value that was sampled. |
| [WEB-0002](../apps/web/decisions/WEB-0002-a-voxel-town-as-a-view-over-a-gl-free-scene-model.md) | accepted | `packages/world/**`<br>`apps/web/src/components/WorldHero.tsx`<br>`apps/web/src/components/WorldExplorer.tsx`<br>`apps/web/src/app/world/**`<br>`apps/web/e2e/a-world.spec.ts` | A voxel town on the landing page, as a view over a GL-free scene model | `@jeve/world` exports `mountWorld(container, options)`, used twice: a hero with no controls and an automatic camera, and an explorer with pan, zoom and clicks. Positions, interpolation and picking live in `WorldModel`, which never touches WebGL; `WorldView` renders it with an orthographic isometric camera, one instanced mesh for every static voxel and one per body part, flat-shaded, all geometry procedural. |
| [WEB-0003](../apps/web/decisions/WEB-0003-draw-nothing-off-screen-and-cheaply-on-software-gl.md) | accepted | `packages/world/src/render.ts`<br>`packages/world/src/index.ts` | Draw nothing off-screen, and draw cheaply on a software renderer | The scene is not drawn while its canvas is off-screen or the tab is hidden, and when WebGL turns out to be a CPU rasteriser it is drawn without antialiasing, at 1x pixel ratio, fifteen times a second. The model advances regardless: positions are what tests and clicks read, and they must not depend on whether anyone is watching. |
| [WEB-0004](../apps/web/decisions/WEB-0004-the-town-is-lit-shadowed-and-peopled-like-a-toy-not-a-diagram.md) | accepted | `packages/world/src/render.ts`<br>`packages/world/src/voxels.ts`<br>`packages/world/src/sky.ts`<br>`packages/world/src/model.ts`<br>`py/src/jeve/world/map.py`<br>`packages/contracts/src/world.ts` | The town is lit, shadowed and peopled like a toy, not a diagram | The look is computed, not post-processed: occlusion and colour jitter are baked per corner when the voxels are built, sky and sun are a pure function of the sim clock, shadow maps are for real GPUs only, and people are posed from four optional fields on the scene model. |
| [WEB-0005](../apps/web/decisions/WEB-0005-the-site-is-a-static-export-nothing-server-renders.md) | accepted | `apps/web/**`<br>`scripts/e2e.sh`<br>`infra/docker/Dockerfile.web` | the site is a static export; nothing server-renders | `next.config.ts` sets `output: "export"`. `page.tsx` renders a static shell; a client component fetches `fetchState`/`fetchLatestEvents` on mount and the "API is not reachable" copy becomes a client-side state. `NEXT_PUBLIC_JEVE_API` is inlined at build time, so changing it means rebuilding. `wrangler.toml` is assets-only (`directory = "out"`, no `main`, no `binding`). |
| [WEB-0006](../apps/web/decisions/WEB-0006-shadcn-base-ui.md) | accepted | `apps/web/src/components/**`<br>`apps/web/src/app/globals.css`<br>`apps/web/src/app/layout.tsx`<br>`apps/web/components.json`<br>`apps/web/postcss.config.mjs`<br>`apps/web/package.json` | UI components are shadcn on Base UI primitives over the app palette | The component layer is **shadcn (Base UI preset, nova style), vendored into `src/components/ui/`**, with the app's palette kept as the single token source: |
| [WEB-0007](../apps/web/decisions/WEB-0007-reports-keep-the-field-reports-own-look-and-a-published-report-is-frozen-data.md) | accepted | `apps/web/src/components/report/**`<br>`apps/web/src/reports/**`<br>`apps/web/src/app/reports/**`<br>`apps/web/e2e/reports.spec.ts` | Reports keep the field report’s own look, and a published report is frozen data | Report pages render through `components/report/`: the artifact's markup, its four chart forms and its town, ported to React over data props, styled by `report.css` scoped under `.rpt`, and set in its own typefaces (loaded by a stylesheet link, not `next/font`, for WEB-0006's reason). |
| [WEB-0008](../apps/web/decisions/WEB-0008-the-hero-camera-is-a-director-not-a-clock.md) | accepted | `packages/world/src/attention.ts`<br>`packages/world/src/index.ts`<br>`packages/world/test/attention.test.ts`<br>`apps/web/src/components/WorldHero.tsx` | The hero camera is a director, not a clock | The hero's camera watches `WorldModel` every frame and looks wherever people are: occupied rooms, the street while anyone is crossing it, a fresh conversation, a just-fired escalation — and the whole town when nobody is out. Empty buildings are never a shot. |
| [WORLD-0001](../py/src/jeve/world/decisions/WORLD-0001-rules-first-then-model.md) | accepted | `py/src/jeve/world/**`<br>`py/src/jeve/decide/policy.py` | Build and tune the world on rules before wiring in any model | Every agent choice goes through a `Policy`, and `RulesPolicy` — deterministic, free, no network — is the first implementation; the model swap is a second implementation of the same protocol, changing nothing upstream. |
| [WORLD-0002](../py/src/jeve/world/decisions/WORLD-0002-a-tick-owns-its-transaction-and-its-sequences.md) | accepted | `py/src/jeve/world/engine.py`<br>`py/src/jeve/db.py`<br>`py/tests/test_resume.py`<br>`py/tests/test_daemon.py` | A tick owns its transaction, and puts its sequences back | `Engine.tick()` ends any open transaction and then does all of its reads and writes inside one real `BEGIN...COMMIT`, so a tick is durable and visible to other sessions the moment it returns. Every serial column's sequence is reset to `max+1` when an engine starts and after any tick that raises. |
| [WORLD-0003](../py/src/jeve/world/decisions/WORLD-0003-space-is-load-bearing.md) | superseded | `py/src/jeve/world/space.py`<br>`py/src/jeve/world/map.py`<br>`py/migrations/0003_space.sql`<br>`py/tests/test_space.py` | Space is load-bearing — zones, encounters, and an escalation that reaches billing | Each open tick, each member of staff makes one `agent.tick` decision from where they stand now: where to go next, whether to talk, to whom, about what, their mood, and — only when it is possible — whether to press the vendor about an outage. Encounters resolve among people co-located *now*; then everyone moves. |
| [WORLD-0004](../py/src/jeve/world/decisions/WORLD-0004-four-more-flows-each-carrying-a-cascade-somewhere-new.md) | accepted | `py/src/jeve/world/flows.py`<br>`py/tests/test_flows.py`<br>`py/src/jeve/core/clock.py` | Four more flows, each built to carry a cascade somewhere new | Four flows, behind the same `Policy` seam as the first six, each with a Jev question set, a rules twin, and tests that fail without it. In every one the model decides a judgement; amounts, eligibility and whether the cash exists are rules. |
| [WORLD-0005](../py/src/jeve/world/decisions/WORLD-0005-the-economy-closes.md) | accepted | `py/src/jeve/world/**`<br>`py/src/jeve/decide/questions.py`<br>`py/src/jeve/decide/policy.py`<br>`py/migrations/**` | The economy closes - tickets end, months recur, bills get answered, wages come back | - **Tickets end.** An answered ticket is confirmed by its reporter (a decision) once the module is back, or closes by rule after two days. The same person hitting the same module within three days reopens it rather than filing anew. - **Months recur.** `month.end`, `close.run`, payroll and catering reschedule themselves; scheduled work is a registry of handlers, each marked `office_hours_only` or not, replacing a nine-branch `if`. - **Every bill is answered once a day.** From two days before it is due, each unpaid invoice gets one `payment.timing` question per sim-day, at an hour that belongs to the payer (CORE-0009), sampled once — not a per-tick hazard. The per-tick `LIMIT` windows go, and with them 1,591 `payment.deferred` events that recorded a die being rolled. A bill seven days late is chased by its issuer (`chase.invoice`, where `vocality` finally reaches a question). Clients in the four prompter quintiles pay by standing instruction on the due date: a gate, not a question. - **Wages come back.** Staff are paid into a household account and spend from it at the cafe. Households are accounts without an org, funded by an opening balance against `external`; payroll is four legs. - **The books open mid-story.** The seed includes last month's invoices falling due in the first week, so collection is visible inside a ten-day run. Initial conditions, not outcomes. - **Gates are written once** (`decide/gates.py`) and read by both policies: not due, cannot afford, already open. - **`make soak`** runs 35 days on rules and asserts invariants, not outcomes: the ledger balances; tickets opened in week five; a second month-end and its close; no receivable older than sixty days unanswered; every firm that trends to zero has an `insolvency.warning` that names why; every counterparty segment acts. It writes `ops/soak.md`. If behaviour sinks a firm, that is reported. |
| [WORLD-0006](../py/src/jeve/world/decisions/WORLD-0006-a-meeting-with-a-stake-gets-rounds.md) | superseded | `py/src/jeve/world/episodes.py`<br>`py/src/jeve/world/space.py`<br>`py/migrations/0009_episodes.sql`<br>`py/tests/test_episodes.py`<br>`py/src/jeve/sim/episode_study.py` | Give a meeting with a stake rounds instead of one shot | A meeting becomes an **episode** — two to four people, up to three rounds, one typed request per participant per round — only when they have something between them: a broken module one can act on, a live bill between their firms, or news one holds and another lacks. Everyone else keeps the one-shot encounter, which stays the control arm and the cheap proxy. Episodes are **off by default**. |
| [WORLD-0007](../py/src/jeve/world/decisions/WORLD-0007-run-episodes-in-every-world-with-no-switch.md) | accepted | `py/src/jeve/world/episodes.py`<br>`py/src/jeve/world/engine.py`<br>`py/src/jeve/sim/daemon.py`<br>`py/src/jeve/sim/soak.py`<br>`py/tests/test_episodes.py` | Run episodes in every world, with no switch | Episodes run in every world the daemon runs, with no flag, environment variable or make variable. The mechanism is WORLD-0006's, with the outage stake corrected (below). |
| [WORLD-0008](../py/src/jeve/world/decisions/WORLD-0008-rounds-remember-and-recurse-two-deep.md) | accepted | `py/src/jeve/world/episodes.py`<br>`py/src/jeve/decide/questions.py`<br>`py/src/jeve/decide/policy.py`<br>`py/src/jeve/memory/store.py`<br>`py/migrations/0010_episode_depth_and_stall.sql`<br>`py/tests/test_episodes.py` | Conversations remember their rounds, end when people are done, and recurse two deep | - **Rounds remember.** Every round's state says what each person did in the round just gone (by the same neutral labels) and how long they have been at it, so round N depends on round N−1. - **People end conversations.** Each person is asked whether they have had their say, not whether the matter is solved. A round in which everyone repeats their last act ends the episode as `stalled`, as the recursion research pre-registered ("rounds continue only on state change"). - **Two deep, by dependency.** `MAX_DEPTH = 2`, in code and in migration 0010. After an episode closes, a pair who have a *different* outage or bill between them take it aside (never a matter an ancestor was about), and news can ripple a second table further. Both count against the daily ceiling. - **Memory is read back.** How someone heard of an outage reaches their first decision to report it; a payer's last kept or broken promise to a firm reaches the next conversation about a bill. Both are appended only when present, so first-hand and first-time questions keep their bytes. |
| [WORLD-0009](../py/src/jeve/world/decisions/WORLD-0009-ask-agent-tick-at-decision-points-and-describe-a-room-as-firms.md) | accepted | `py/src/jeve/world/space.py`<br>`py/src/jeve/decide/questions.py`<br>`py/src/jeve/core/orgs.py`<br>`py/migrations/0011_decision_points.sql`<br>`py/tests/test_space.py`<br>`py/tests/test_questions.py` | Ask agent.tick at decision points, and describe a room as firms | Somebody is asked `agent.tick` on arrival, while away from their workplace, in company from another firm, when what is on their mind changes, through lunch, and on the hour; cafe staff alone behind their counter are not asked. A quiet desk is asked about the next hour, everyone else about the next quarter. |
| [WORLD-0010](../py/src/jeve/world/decisions/WORLD-0010-give-the-economy-sinks-and-consequences.md) | accepted | `py/src/jeve/world/economy.py`<br>`py/src/jeve/world/flows.py`<br>`py/src/jeve/world/seed_world.py`<br>`py/src/jeve/sim/soak.py`<br>`py/tests/test_consequences.py` | Give the economy sinks and consequences | Rent, stock, quarterly tax on profit and household spending are rules; staff two paydays unpaid decide whether to leave (`leave.consider`), a firm's head reviews its position monthly and on every warning (`founder.review`: raise prices, cut costs, chase debts, borrow, carry on), vacancies are refilled by decision (`hire.decision`), and a firm four paydays behind has failed. |
| [WORLD-0011](../py/src/jeve/world/decisions/WORLD-0011-give-every-firm-the-loop-the-scenario-names.md) | accepted | `py/src/jeve/world/engineering.py`<br>`py/src/jeve/world/customers.py`<br>`py/src/jeve/world/timesheets.py`<br>`py/src/jeve/world/shocks.py`<br>`py/src/jeve/world/flows.py`<br>`py/migrations/0012_loops.sql`<br>`py/tests/test_loops.py` | Give every firm the loop the scenario names | Each firm's loop is a decision at the moment it arises, with a rules twin and its consequences in code: engineering allocation and deploys (debt is live and drives the hazard), trust revised after an outage and renewal asked of customers at risk, a workaround chosen with every report, disputes and their resolution, time logged or not at the law firm, the cafe's cover, stock and catering acceptance, and the close's wait/nag/estimate. Ledgerline closes two clients a day and three fall due together, so its principal decides whose month waits (`close.plan`, a quarter of an hour ahead: the scheduler takes a tick's due rows off the queue before running any). Shocks (Poisson 1.5 a week, keyed by the week) are the environment they answer to. |
| [WORLD-0012](../py/src/jeve/world/decisions/WORLD-0012-route-an-escalation-through-whoever-was-cornered.md) | accepted | `py/src/jeve/world/episodes.py`<br>`py/src/jeve/world/engine.py`<br>`py/tests/test_loops.py` | Route an escalation through whoever was cornered | A complaint raised with an engineer (the founder, an engineering lead, an engineer or an SRE) shortens the outage at once, as before; raised with anyone else, it becomes an `escalation.handoff` decision a quarter of an hour later — relay it to engineering, or leave it in the queue. Support escalates by rule once three blocked customers' tickets are triaged against one outage, and a customer can ring their account manager as a workaround (WORLD-0011). |
| [WORLD-0013](../py/src/jeve/world/decisions/WORLD-0013-decisions-and-events-carry-their-inputs-reasons-and-pressure.md) | accepted | `py/migrations/0015_signal.sql`<br>`py/src/jeve/world/engine.py`<br>`py/src/jeve/world/space.py`<br>`py/src/jeve/decide/questions.py`<br>`py/src/jeve/evals/metrics.py`<br>`py/tests/test_field_report.py` | Decisions and events carry their inputs, reasons and pressure | Every decision stores the facts it was asked on (`decisions.facts`). A late bill carries a typed reason, and the pressure put on it is recorded: how often it was chased, whether it was raised in person, and any promise to pay. Events name what their consumers need to explain them. |
| [WORLD-0014](../py/src/jeve/world/decisions/WORLD-0014-calibrate-to-cited-data-and-make-a-monthly-base-rate-a-hazard-not-a-question.md) | accepted | `py/src/jeve/world/engine.py`<br>`py/src/jeve/world/economy.py`<br>`py/src/jeve/world/customers.py`<br>`py/src/jeve/decide/policy.py`<br>`py/src/jeve/evals/priors.py`<br>`py/tests/test_loops.py` | Calibrate to cited data, and make a monthly base rate a hazard, not a question | Rates are set from cited data. A month's base rate for people with nothing pushing them is a rule hazard. Jev is asked only when something in the situation pushes. |
