---
description: CORE-0001: Record architecture decisions as immutable per-file records. Decisions are MADR 4.0 records, one per file, named `<PREFIX>-NNNN-kebab.md`, with machine-readable front matter; `INDEX.md`, `index.jsonl`, `schema.json` and the Cursor rules are generated from them and never hand-edited. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: process, documentation, agents.
globs: decisions/**,**/decisions/*.md,scripts/gen-decisions.py
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from decisions/core/CORE-0001-record-architecture-decisions.md. -->

# CORE-0001 — Record architecture decisions as immutable per-file records

Status: accepted (2026-09-20). The record is canonical: decisions/core/CORE-0001-record-architecture-decisions.md

Decisions are MADR 4.0 records, one per file, named `<PREFIX>-NNNN-kebab.md`, with machine-readable front matter; `INDEX.md`, `index.jsonl`, `schema.json` and the Cursor rules are generated from them and never hand-edited.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
