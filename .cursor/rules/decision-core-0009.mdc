---
description: CORE-0009: Randomness is keyed by what it is about, never by when it was drawn. A seed path names the thing the draw is *about* and, where the thing recurs, its own period: an invoice amount is `(issuer, client, billing month)`; whether a subscriber notices an outage is `(person, incident)`; the hour a payer sits down to their bills is `(person, day)`; the hazard is `(module, day, slot)`; where someone stands is `(person, zone, arrival)`. `tick_seq` and event sequence numbers never appear in a seed path, and a test walks the source to keep it so. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: determinism, randomness, counterfactuals, agent-decided.
globs: py/src/jeve/world/**,py/src/jeve/core/seed.py,py/tests/test_world.py
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from decisions/core/CORE-0009-randomness-is-keyed-by-what-it-is-about.md. -->

# CORE-0009 — Randomness is keyed by what it is about, never by when it was drawn

Status: accepted (2026-09-20). The record is canonical: decisions/core/CORE-0009-randomness-is-keyed-by-what-it-is-about.md

A seed path names the thing the draw is *about* and, where the thing recurs, its own period: an invoice amount is `(issuer, client, billing month)`; whether a subscriber notices an outage is `(person, incident)`; the hour a payer sits down to their bills is `(person, day)`; the hazard is `(module, day, slot)`; where someone stands is `(person, zone, arrival)`. `tick_seq` and event sequence numbers never appear in a seed path, and a test walks the source to keep it so.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
