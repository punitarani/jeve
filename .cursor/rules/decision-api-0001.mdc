---
description: API-0001: Make the event seq the only cursor the client tracks. Every endpoint that returns events also returns the `seq` it is current as of, and `/stream` takes `after=<seq>`, so a reconnect is exactly "give me what comes after this" with no gap and no duplicate. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: api, streaming, agent-decided.
globs: py/src/jeve/api/app.py,apps/web/src/lib/api.ts
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from py/src/jeve/api/decisions/API-0001-seq-cursor-everywhere.md. -->

# API-0001 — Make the event seq the only cursor the client tracks

Status: accepted (2026-09-20). The record is canonical: py/src/jeve/api/decisions/API-0001-seq-cursor-everywhere.md

Every endpoint that returns events also returns the `seq` it is current as of, and `/stream` takes `after=<seq>`, so a reconnect is exactly "give me what comes after this" with no gap and no duplicate.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
