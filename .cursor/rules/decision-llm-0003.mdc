---
description: LLM-0003: Route with per-request provider preferences, never account settings. Routing preferences are set per request in the body — ordering, sort, ignore list — and account-level settings are left alone; if one ever must change, the before-state is written to `ops/provider-settings.before.json` first. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: openrouter, routing, operations.
globs: py/src/jeve/llm/protocol.py
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from py/src/jeve/llm/decisions/LLM-0003-per-request-provider-preferences.md. -->

# LLM-0003 — Route with per-request provider preferences, never account settings

Status: accepted (2026-09-20). The record is canonical: py/src/jeve/llm/decisions/LLM-0003-per-request-provider-preferences.md

Routing preferences are set per request in the body — ordering, sort, ignore list — and account-level settings are left alone; if one ever must change, the before-state is written to `ops/provider-settings.before.json` first.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
