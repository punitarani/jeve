---
description: SIM-0002: The daemon outlives its model - wait, say so, retry the tick. The run loop treats `TransportError`, `ResponseShapeError` and `TimeoutError` as weather. The tick is rolled back, `sim_meta.status` becomes `waiting_on_model` with the error's class and first line in `last_error`, and the same tick is tried again after a jittered backoff that doubles to a cap of two minutes. The first tick that succeeds sets `running` and clears the error. Budget exhaustion and a replay miss keep their own statuses and exits: they are not weather, and retrying them cannot help. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: daemon, robustness, backpressure, agent-decided.
globs: py/src/jeve/sim/daemon.py,py/src/jeve/sim/runner.py,py/src/jeve/api/app.py,py/migrations/**
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from py/src/jeve/sim/decisions/SIM-0002-the-daemon-outlives-its-model.md. -->

# SIM-0002 — The daemon outlives its model - wait, say so, retry the tick

Status: accepted (2026-09-20). The record is canonical: py/src/jeve/sim/decisions/SIM-0002-the-daemon-outlives-its-model.md

The run loop treats `TransportError`, `ResponseShapeError` and `TimeoutError` as weather. The tick is rolled back, `sim_meta.status` becomes `waiting_on_model` with the error's class and first line in `last_error`, and the same tick is tried again after a jittered backoff that doubles to a cap of two minutes. The first tick that succeeds sets `running` and clears the error. Budget exhaustion and a replay miss keep their own statuses and exits: they are not weather, and retrying them cannot help.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
