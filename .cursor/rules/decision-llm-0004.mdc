---
description: LLM-0004: Guard spend with reservations in a locked ledger at the repo root. Every call reserves its worst-case cost in an append-only locked ledger before it is issued and settles at the real cost afterwards; effective spend counts settled costs plus every reservation that never settled. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: cost, safety, agent-decided.
globs: py/src/jeve/llm/budget.py,py/src/jeve/llm/ledger.py
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from py/src/jeve/llm/decisions/LLM-0004-budget-guard.md. -->

# LLM-0004 — Guard spend with reservations in a locked ledger at the repo root

Status: accepted (2026-09-20). The record is canonical: py/src/jeve/llm/decisions/LLM-0004-budget-guard.md

Every call reserves its worst-case cost in an append-only locked ledger before it is issued and settles at the real cost afterwards; effective spend counts settled costs plus every reservation that never settled.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
