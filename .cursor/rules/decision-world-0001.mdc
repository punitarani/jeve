---
description: WORLD-0001: Build and tune the world on rules before wiring in any model. Every agent choice goes through a `Policy`, and `RulesPolicy` — deterministic, free, no network — is the first implementation; the model swap is a second implementation of the same protocol, changing nothing upstream. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: world, testing, agent-decided.
globs: py/src/jeve/world/**,py/src/jeve/decide/policy.py
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from py/src/jeve/world/decisions/WORLD-0001-rules-first-then-model.md. -->

# WORLD-0001 — Build and tune the world on rules before wiring in any model

Status: accepted (2026-09-20). The record is canonical: py/src/jeve/world/decisions/WORLD-0001-rules-first-then-model.md

Every agent choice goes through a `Policy`, and `RulesPolicy` — deterministic, free, no network — is the first implementation; the model swap is a second implementation of the same protocol, changing nothing upstream.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
