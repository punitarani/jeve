---
description: CORE-0004: Slow the clock under backpressure; never default a decision. The tick is a barrier: sim time does not advance until every decision due at that tick has a real answer, so a slow model means a slow world and a dead model means a paused world. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: backpressure, reliability, clock.
globs: py/src/jeve/llm/**
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from decisions/core/CORE-0004-backpressure-slows-the-clock.md. -->

# CORE-0004 — Slow the clock under backpressure; never default a decision

Status: accepted (2026-09-20). The record is canonical: decisions/core/CORE-0004-backpressure-slows-the-clock.md

The tick is a barrier: sim time does not advance until every decision due at that tick has a real answer, so a slow model means a slow world and a dead model means a paused world.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
