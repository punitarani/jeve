---
description: WEB-0003: Draw nothing off-screen, and draw cheaply on a software renderer. The scene is not drawn while its canvas is off-screen or the tab is hidden, and when WebGL turns out to be a CPU rasteriser it is drawn without antialiasing, at 1x pixel ratio, fifteen times a second. The model advances regardless: positions are what tests and clicks read, and they must not depend on whether anyone is watching. Consult before changing this area, or when asking why it works this way, what was decided, or whether it can be changed. Topics: three.js, performance, testing, agent-decided.
globs: packages/world/src/render.ts,packages/world/src/index.ts
alwaysApply: false
---

<!-- GENERATED by scripts/gen-decisions.py from apps/web/decisions/WEB-0003-draw-nothing-off-screen-and-cheaply-on-software-gl.md. -->

# WEB-0003 — Draw nothing off-screen, and draw cheaply on a software renderer

Status: accepted (2026-09-20). The record is canonical: apps/web/decisions/WEB-0003-draw-nothing-off-screen-and-cheaply-on-software-gl.md

The scene is not drawn while its canvas is off-screen or the tab is hidden, and when WebGL turns out to be a CPU rasteriser it is drawn without antialiasing, at 1x pixel ratio, fifteen times a second. The model advances regardless: positions are what tests and clicks read, and they must not depend on whether anyone is watching.

This decision is immutable. To change it, write a new record and set `superseded-by` on this one — do not edit its substance.
